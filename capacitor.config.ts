import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jsmobiledev.jorge',
  appName: 'party-planner',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
